#########
#  SLL e SLR -> Applicando l'istruzione ad 8 bit (1 byte), riempie poi il buco con uno zero.
#  SRA funziona solo a destra; il bit di peso maggiore viene riempito con il bit di peso maggiore (in origine)
#  |
#  | -> Riesce a mantenere il segno